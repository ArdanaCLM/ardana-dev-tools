{% set _dplyr = input_model_deployer | default(input_model_servers[0].id) %}
---
all:
  children:
{% if _dplyr != 'deployer' %}
    deployer:
      hosts:
        "{{ _dplyr }}":
{% endif %}
    cloud:
      hosts:
{% for server in input_model_servers %}
        "{{ server.id }}":
{% endfor %}
